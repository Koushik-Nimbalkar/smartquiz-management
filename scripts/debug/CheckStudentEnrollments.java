import com.smartquiz.config.DBConnection; import java.sql.*; public class CheckStudentEnrollments { public static void main(String[] args) throws Exception { try (Connection conn = DBConnection.getConnection()) { int studentId = 5; // test@student.com try (PreparedStatement ps = conn.prepareStatement("SELECT c.name FROM enrollments e JOIN classes c ON e.class_id = c.id WHERE e.student_id = ?")) { ps.setInt(1, studentId); ResultSet rs = ps.executeQuery(); System.out.println("Student is enrolled in:"); while (rs.next()) { System.out.println("- " + rs.getString("name")); } } } } }
